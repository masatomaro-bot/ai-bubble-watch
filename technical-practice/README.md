# 練習テクニカル・トラッキングツール

## 更新履歴
- 2026.09.17: Fable 5.1によるダッシュボードレビューを受けての改善
  (「毎日の定点観測で、株式が調子が悪くなる前兆に気づき心の準備ができるように
  したい」という目的に対して)
  - **データ信頼性の改善**: 母集団サイズが日によって大きく変動していた問題
    (`universe.py`の`_fetch_url`にリトライを追加)、`result["date"]`が実行日
    でなく対象取引日になるよう修正、Market Leadersの異常値(分割未調整・
    超低位株)を除外するフィルタ(`select_market_leaders`)を追加
  - **市場ストレス指標を追加**(`compute_stress_gauges`): VIX水準、
    HYG/IEF(信用選好)・RSP/SPY(値がさ株への集中度)・IWM/SPY(小型株選好)・
    SOXX/SPY(半導体選好)の各比率と直近10営業日の変化
  - **警戒チェックリストを追加**(`compute_warning_flags`): 指数高値圏での
    ブレッドス弱含み、新安値>新高値、売り抜け日数過多、リーダー株の崩れ、
    ディフェンシブへのローテーションの5パターンを機械判定(未検証・参考情報)
  - **推移スパークラインをダッシュボードに追加**: 累積A/Dライン・50日線上
    比率・新高値-新安値・VIXの推移をインラインSVGで表示(サーバー側計算なし、
    既存の日次履歴を再利用)
  - **GitHub Issue自動通知を追加**(`notify_changes.py`): 総合判定の変化・
    警戒フラグの新規点灯時にIssueを自動作成(標準のGITHUB_TOKENのみで動作)
  - **前営業日との比較セクション・経済カレンダー(TradingView公式ウィジェット)・
    慈愛の観察台v4へのリンクをダッシュボードに追加**(2026.09.15-17の一連の対応)
- 2026.09.14(その2): 判定ロジックの見直し・履歴のgit管理化・ダッシュボード追加
  （「セクター4分類の閾値が完全に自前設計で検証もされていない」という
  レビュー指摘を受けての対応）
  - **O'Neil/IBD式フォロースルー・デイ(FTD)判定を追加し、主判定に格上げ**
    (`compute_trend_state`)。「確認された上昇トレンド/圧力下の上昇トレンド/
    調整局面」の3状態を、反発初日からの日数・上昇率・出来高という公開された
    定義に基づいて判定する。セクター4分類の多数決(`market_regime`)は
    参考表示に格下げ(オニミネの非公開ロジックの推測である点は変わらないため)
  - **広域ユニバース全体のテクニカル比率を追加**
    (`compute_broad_universe_technicals`): 50日線/200日線上比率、トレンド
    テンプレート8条件合格率(RS百分位proxyを含む)。既にブレッドス計算で
    取得済みの数千銘柄分の価格データを再利用するため追加コストは小さい
  - **累積A/Dライン**(値上がり銘柄数-値下がり銘柄数の累積)を追加。前日分は
    Google Sheetsではなく後述の履歴JSONから読む
  - **Google Sheetsの書き込みをupsert化**。手動再実行やcronの重複起動で
    同じ日付の行が何行も増えていた問題(2026-09-13に発覚)を解消
    (`sheets_writer.upsert_row`)
  - **「gitをDBにする」履歴JSON保存を追加**(`history_store.py`)。日次結果を
    `docs/data/history/YYYY-MM-DD.json` として保存し、GitHub Actionsが
    自動でコミット・プッシュする(要`permissions: contents: write`)
  - **GitHub Pages用の静的ダッシュボードを追加**(`docs/index.html`)。上記
    履歴JSONを読んで表示する、ビルド不要の素のHTML/JS。閲覧にはリポジトリの
    Pages設定を一度有効にする必要がある(下記セットアップ参照)
  - **判定ロジックの過去検証スクリプトを追加**(`backtest_trend_state.py`)。
    FTD判定の3状態それぞれについて、その後N営業日のリターンの平均・中央値・
    勝率を集計できる。実データでの検証はまだ行っていない(下記「重要な制約」参照)
  - 単体テスト48件(合成データ+モック、run()自体の結合テストも追加)
- 2026.09.14: 定期実行(cron)failureの修正
  - PR #7マージ後、初回の定期実行(cron)が`['^IXIC']: OperationalError('database
    is locked')`で失敗(2026-09-14T00:02 UTC、run #13)。2026-09-10の定期実行でも
    同一エラーで失敗しており(`^GSPC`)、yfinance内部キャッシュ(cookie/crumb
    情報のsqlite DB)への同時アクセスによる既知の断続的エラーと判断
  - `workflow_dispatch`の手動実行では今のところ再現していないが、原因は
    特定できていない偶発的なロック競合と考えられるため、"database is locked"
    エラーのみを対象にリトライする共通ヘルパー`yf_retry.py`を新設
  - `market_climate.py`・`ffty_screener.py`双方のyfinance呼び出し箇所を
    このヘルパー経由に統一(両モジュールは相互にimportし合っているため、
    循環importを避けて独立モジュールに切り出した)
  - 次回の定期実行(cron)で改善するか要確認
- 2026.09.13(その2): GitHub Actionsでの実機検証結果を反映
  - **iShares(IWV/ITOT)は実際に動かした結果、CSVではなくサイトの通常の
    HTMLページが返ってくることを確認(サイト側の仕様変更でURLが失効した
    と判断)。** 実用上はもう機能しないものとして扱い、NASDAQ Trader経由の
    取得(下記)を主なフォールバック先として運用する
  - NASDAQ Trader公式シンボル一覧経由の取得が実機で成功を確認
    (`breadth_universe_source: "nasdaqtrader"`、384銘柄で稼働)
  - `max_breadth_tickers`で母集団を絞る際、単純な先頭切り出しだと
    ティッカー一覧がアルファベット順のため「A〜B台の銘柄」に偏り、Market
    Leaderの結果が明らかに偏る不具合を発見・修正(`sample_tickers`で固定
    シードのランダムサンプリングに変更)
  - ブレッドス用の価格データ取得期間を"1y"→"2y"に変更(RS百分位計算に
    必要な252営業日分が"1y"だと僅かに不足し、Market Leaderが空になる
    不具合の修正)
- 2026.09.13: 地合い判定を拡張(オニミネサイトの構成に合わせて優先度順に実装)
  - ブレッドス(市場の幅)の母集団をS&P500(~500銘柄)から、iShares
    Russell 3000 ETF(IWV)の保有銘柄をベースにした広域ユニバース(数千銘柄
    規模)に拡張。取得失敗時はS&P500に自動フォールバック(`universe.py`)
  - 厳密な新高値/新安値銘柄数、値上がり/値下がり銘柄数、出来高ブレッドを追加
    (`compute_breadth_extended`)
  - Market Leader(個別銘柄RSランキング)を追加。広域ユニバース内でのトレー
    リング1年リターン百分位を代替指標として使用し、上位20銘柄をSheetsの
    別シート(`Market Leaders`)に出力
  - セクター温度感(上昇/買い集め/中立/調整)の4分類と、その多数決による
    RISK-ON/RISK-OFF/MIXED判定を追加(`classify_sector_temperature`,
    `majority_vote_market_regime`) — オニミネの「本日の判定」に相当する
    部分。分類の閾値は完全に自前設計(下記「重要な制約」参照)
  - テーマ・ローテーション(11 GICSセクターより細かい約12テーマの代理ETFに
    よるRRG分類)を追加(`THEME_ETFS`) — オニミネの「産業セクター月間
    パフォーマンス」相当だが、正確な369テーマ分類とは別物の近似
  - **経済イベントカレンダーは未実装。** FOMC/CPI/PPI/雇用統計等の具体的な
    日付を裏付けなく記載するとユーザーの「数字を作らない」という運用方針に
    反するため、今回は着手を見送った。実装する場合は連邦準備制度理事会
    (FOMC日程)やBLS(CPI/PPI/雇用統計の発表日程)が公式に事前公開している
    情報を都度取得する設計にすること(手打ちの推測日付は入れない)
- 2026.09.06: 実機(macOS)での初回実行で見つかった2件を修正
  - 予算フィルタが「1株30万〜100万円」という誤ったレンジ判定になっていたバグを修正
    (正しくは「1銘柄への投資額の目安が30万〜100万円」であり、1株価格には上限
    100万円のチェックのみ適用する。参考情報として予算上下限それぞれで買える
    株数も出力するようにした)
  - S&P500銘柄リスト取得がWikipediaから403 Forbiddenで失敗する問題を修正
    (User-Agentヘッダーを付与)

慈愛の観察台(定点観測)とは別建ての「練習テクニカル」(O'Neil/Minervini/VCP方式の
小さめポジションでの練習トレード)用に、次の2つを毎営業日自動実行するスクリプト。

1. `market_climate.py` — 地合い(市場全体の状態)を毎日記録する
2. `ffty_screener.py` — FFTY(CapForce IBD 50 ETF)構成銘柄をミネルヴィニ・
   トレンドテンプレートでスクリーニングし、予算内の候補を記録する

VCPやカップ・ウィズ・ハンドルなどベースパターンの視覚判定は行わない。
そこはチャートをTradingViewで自分の目で見て判断する、という方針のまま。
このツールがやるのは「トレンドテンプレートを通過した監視対象」を毎日
機械的にログに残すところまで。

---

## 1. 何を計算しているか

### market_climate.py (地合い)

オニミネサイトのトップページに書かれている定義をそのまま実装した部分と、
自分で近似した部分がある。

| 項目 | 実装内容 | 出典/根拠 |
|---|---|---|
| **地合いの主判定: フォロースルー・デイ(FTD)3状態** | NASDAQ・S&P500それぞれについて、反発初日(安値後最初の陽線)からDay4〜10の間に+1.25%以上かつ前日比出来高増の日(FTD)が出れば「確認された上昇トレンド」。直近25営業日の売り抜け日数が5日以上なら「圧力下の上昇トレンド」に格下げ。FTDが未確認なら「調整局面」 | O'Neil/IBD(Investor's Business Daily)が公開しているフォロースルー・デイの定義をそのまま実装。安値・反発初日の検出、失敗時の再探索回数などは自前の簡略化あり(`compute_trend_state`のdocstring参照) |
| 売り抜け日数・失速日数 | NASDAQ総合(^IXIC)・S&P500(^GSPC)、直近25営業日、前日比-0.2%以下+出来高増=売り抜け、+0.2%未満の小幅高+出来高増+安値圏引け=失速、25営業日経過 or +5%ラリーで除外 | オニミネのトップページ注記をそのまま実装 |
| 「安値圏引け」の定義 | 当日レンジの下位25%以内で引けたこと、と独自に定義 | オニミネ側は数値非公開のため独自解釈(要調整) |
| セクター・ローテーション | 11 SPDRセクターETFの相対力を簡易RRG(RS-Ratio/RS-Momentum)で4象限に分類 | 一般的なJdK RRG方式の簡易実装(厳密な計算式とは異なる近似) |
| テーマ・ローテーション | GICS11セクターと重複しない約12のテーマ型/サブセクター型ETF(半導体・クリーンエネ・宇宙防衛・原子力等)を同じRRG方式で分類 | オニミネの「産業セクター月間パフォーマンス」(約15テーマ)に相当。正確な構成銘柄・テーマ数は非公開のため近似 |
| セクター体温(参考表示) | 11セクターETFそれぞれを「上昇/買い集め/中立/調整」の4分類にし、多数決でRISK-ON/RISK-OFF/MIXEDを判定 | オニミネの「セクター体温の多数決」という考え方を再現した参考指標。**分類の閾値・ロジックは完全に自前設計**でオニミネの実際の判定と一致する保証はないため、2026.09.14以降は主判定(FTD)の補助情報という位置づけに変更 |
| 市場の幅(ブレッドス) | 広域ユニバース(NASDAQ Traderベース、取得失敗時はS&P500にフォールバック)のうち、厳密な新52週高値/安値銘柄数、値上がり/値下がり銘柄数、出来高ブレッド、52週高値/安値2%以内の比率、**累積A/Dライン**(値上がり-値下がりの累積) | オニミネは全米国株(~4465銘柄)が母集団。母集団は完全一致しないため、**水準はオニミネの数字と直接比較できない** |
| 広域ユニバースの技術比率 | 母集団全体のうち50日線/200日線を上回る比率、ミネルヴィニ・トレンドテンプレート8条件全合格の比率 | 一般的なO'Neil/ミネルヴィニ流の地合い判断でよく使われる比率指標。8条件目のRSはproxy(下記) |
| Market Leader(個別銘柄RS) | ブレッドス母集団内でのトレーリング1年リターン百分位を上位から20銘柄 | 本物のIBD RSレーティングとは異なる代替指標(proxy)。ffty_screener.pyと同じ考え方 |
| 指数ステージ | 50/150/200日線に対する位置 | 一般的なステージ分析の考え方 |
| 経済イベントカレンダー | 未実装 | 具体的な発表日を裏付けなく記載しない方針のため、着手を見送り中 |

### ffty_screener.py (FFTY×トレンドテンプレート×予算フィルタ)

1. FFTY(CapForce IBD 50 ETF)の構成銘柄一覧を取得(現金・短期国債などは除外)
2. 各銘柄にミネルヴィニ・トレンドテンプレート8条件のうち7条件を計算
   (価格>150日線>200日線、200日線上昇中、50日線が150/200日線より上、
   価格>50日線、52週安値から+25%以上、52週高値から-25%以内)
3. 8条件目(RSレーティング≧70)は本物のIBD RSレーティングを再現できないため、
   **FFTY構成銘柄"内"でのトレーリング1年リターンの百分位**を代替指標として使用
   (上位30%=70点相当、という近似)
4. 為替(USD/JPY)を取得し、1株あたり価格が予算(30万〜100万円)に収まるかを判定
5. 結果を日付付きでログに残す(合格数の多い順)

---

## 2. 重要な制約・注意点(読んでから使うこと)

- **FFTY構成銘柄の取得方法**: FFTYは2026年4月にInnovatorからCapForceへ運用移管
  されており、公式サイト(capforceetf.com)にクリーンなCSV/API公開URLが見当たら
  なかった。そのためPlaywright(ヘッドレスブラウザ)で保有銘柄ページを
  レンダリングしてスクレイピングする方式にしている。サイトのHTML構造が
  変わるとセレクタ調整が必要になる、という前提の実装。
- **このリポジトリを作った環境(サンドボックス)は外部ネットワークに出られず**、
  yfinance・capforceetf.com・iShares(universe.py)への実アクセスでの動作確認
  ができていない。ロジック部分は合成データでのユニットテスト(`tests/`)で
  検証済みだが、**実データでの初回実行結果は必ず目視確認すること**。
- 8条件目のRSレーティングはIBD公式値の代替(proxy)であり、本物のRSレーティング
  とは数値の意味が異なる。Market Leaderの`rs_percentile`も同様。
- ブレッドス(市場の幅)は広域ユニバース(iShares IWV/Russell3000ベース)を
  母集団にした近似値。オニミネの表示する数字と桁や水準を直接比較しないこと。
  取得に失敗した場合はS&P500(~500銘柄)に自動フォールバックするため、実行の
  たびに`breadth_universe_source`(sheetの列 / JSON出力)を確認し、想定外に
  `sp500`側へ落ちていないかをチェックすること。
- **`universe.py`のiShares保有銘柄CSV取得URLは、実機検証(2026-09-13)で
  機能しないことを確認済み。** CSVではなくiSharesサイトの通常のHTMLページ
  が返ってくる(サイト側の仕様変更でURL/ID体系が変わったと考えられる)。
  実用上はNASDAQ Trader経由(nasdaqlisted.txt/otherlisted.txt)のフォール
  バックで運用する前提とし、iSharesのURL修正は優先度を下げている。
  NASDAQ Trader経由は同じ実機検証で取得成功を確認済み。
- **数千銘柄のyfinance一括取得は実行時間・レート制限が未検証。** 初回は
  `workflow_dispatch`の`max_breadth_tickers`を500程度に絞って実行時間と
  エラーの有無を確認してから、段階的に上限を緩める運用を推奨する。
- セクター温度感(上昇/買い集め/中立/調整)とRISK-ON/OFF判定の閾値は、
  オニミネの非公開ロジックを推測したものではなく、**完全に自前で設計した
  基準**。数値の意味自体がオニミネと異なる可能性がある前提で見ること。
- テーマ・ローテーション(THEME_ETFS)は代理ETFによる近似であり、オニミネの
  正確な369テーマ分類・約15の大分類とは対応関係が異なる。
- 「安値圏引け」の閾値(レンジ下位25%)はこちらの独自解釈。実際のオニミネの
  判定と細部が食い違う可能性がある。
- 経済イベントカレンダーは未実装(意図的に見送り)。実装時はFOMC/BLS等の
  公式発表日程を都度取得する設計にし、日付を手打ちで書かないこと。
- **FTD判定(`compute_trend_state`)は原典の定義を可能な限りそのまま実装
  しているが、以下は自前の簡略化。** 安値の検出は単純な「直近ウィンドウ内
  終値最小値」、複数回の反発失敗〜再安値パターンは最大2回までしか再探索
  しない、FTD成立を待つ期間はDay4〜10固定(流派によりDay25まで許容する
  説もある)。詳細はコード内コメント参照
- **`backtest_trend_state.py`による検証はまだ実データで行っていない。**
  このサンドボックスは外部ネットワークに出られないため、合成データでの
  ロジックテストのみ済んでいる。実際にFTD3状態がその後のリターンと
  意味のある関係になっているかは、GitHub Actions・手元・Colab等で
  一度実行して確認すること
- **履歴JSON(`docs/data/history/`)は2026.09.14以降のデータしか存在しない。**
  それ以前の地合いは記録されていないため、長期のダッシュボード表示や
  バックテストの対象期間はこの日以降に限られる(NASDAQ/S&P500自体の
  過去データはyfinanceから遡って取得できるので、`backtest_trend_state.py`
  はこの制約を受けない)
- **GitHub Pagesダッシュボード(`docs/index.html`)は、リポジトリのPages設定を
  手動で一度有効にしないと表示されない。** 下記セットアップ参照
- 定期実行(cron)が`docs/data/history/`をコミット・プッシュするには、
  ワークフローに`permissions: contents: write`が必要(設定済み)。組織の
  ポリシーでActionsのデフォルト権限がread-onlyに固定されている場合は
  別途リポジトリ設定側の許可が必要になることがある

---

## 3. セットアップ

```bash
pip install -r requirements.txt
python -m playwright install --with-deps chromium
```

テスト(`tests/`)も実行したい場合は代わりに `pip install -r requirements-dev.txt` (pytestも入る)。

### Google Sheetsへの書き込みを使う場合

既存の慈愛の観察台パイプラインと同じ、Googleサービスアカウント方式。

1. Google Cloud ConsoleでサービスアカウントJSONキーを発行
2. 書き込み先のスプレッドシートをそのサービスアカウントのメールアドレスに
   共有(編集者権限)
3. 環境変数(またはGitHub Secrets)に以下を設定
   - `GOOGLE_SERVICE_ACCOUNT_JSON` : サービスアカウントJSONの中身(文字列)
   - `SPREADSHEET_ID` : スプレッドシートのID(URLの `/d/` と `/edit` の間の文字列)

Sheetsの環境変数を設定しなければ、標準出力に結果を表示するだけの動作になる
(お試し実行はこちらでOK)。

### GitHub Actionsでの自動実行

`.github/workflows/technical_practice_daily.yml` を既存リポジトリ
(masatomaro-bot/ai-bubble-watch など)に配置し、Secretsに
`GOOGLE_SERVICE_ACCOUNT_JSON` と `SPREADSHEET_ID` を設定すれば、UTC日〜木
22:00 (JST月〜金 朝7:00、直近のUS取引日の終値を反映)に自動実行される。
手動実行したい場合はActionsタブから `workflow_dispatch` で起動でき、その際
`breadth_universe`(broad/sp500)と`max_breadth_tickers`(母集団の件数上限)
を指定できる。**初回実行は`max_breadth_tickers`を500程度に絞って実行時間と
エラーの有無を確認してから、段階的に上限を緩めることを強く推奨する。**

`market_climate.py`単体を手元/CIで実行する場合、以下の環境変数でも同様に
制御できる(`workflow_dispatch`の入力はこの環境変数に渡されるだけ):
- `BREADTH_UNIVERSE` : `broad`(既定) または `sp500`
- `MAX_BREADTH_TICKERS` : 母集団の件数上限(未設定なら無制限)

### GitHub Pagesダッシュボードを有効にする

`docs/index.html` は日次実行が `docs/data/history/` に書き出したJSONを読んで
表示する静的ページ。**リポジトリ側でPagesを一度手動で有効にしないと
公開されない**(これはAPI経由の操作ができず、このツールからは自動化できない)。

1. GitHubのリポジトリ画面で Settings → Pages を開く
2. "Build and deployment" の Source を "Deploy from a branch" にする
3. Branch を `main` 、フォルダを `/docs` にして Save
4. 数分待つと `https://<ユーザー名>.github.io/ai-bubble-watch/` で公開される
   (Settings → Pages の画面にURLが表示される)

日次実行が一度も成功していない、またはPagesがまだ配信を始めていない間は
「データがまだありません」という表示になる(想定内の挙動)。

### 判定ロジックの過去検証(バックテスト)

```bash
python backtest_trend_state.py --ticker ^IXIC --period 5y --forward-days 20
```

FTD3状態(confirmed_uptrend / uptrend_under_pressure / correction)それぞれ
について、判定日からforward-days営業日後の指数リターンの平均・中央値・勝率
を表示する。**まだ実データでは実行していない**(このサンドボックスが外部
ネットワークに出られないため)。GitHub Actions・手元PC・Google Colabの
いずれかで一度実行し、FTD判定が実際に意味のある区別になっているかを
確認してから、セクター温度感などの他の判定ロジックのチューニングに
進むことを推奨する。

### Google Colabで対話的に試す

`notebooks/market_climate_explorer.ipynb` は、MacでもPCでもブラウザだけで
開けるGoogle Colab用のノートブック。`market_climate.py`と同じ関数を使い、
セクター/テーマのRRGマップを散布図で見たり、セクター温度感の閾値
(`RS_TREND_WINDOW`等)を変えて再分類結果を試したりできる。日次自動実行には
影響しない、あくまで手元での確認・調整用の場所。Colabで「ファイル」→
「ノートブックを開く」→「GitHub」からこのリポジトリのURLを指定して開く。

---

## 4. ローカルで試す

```bash
python market_climate.py    # 地合いをJSON表示 (+Sheets書き込み、履歴JSON保存)
MAX_BREADTH_TICKERS=500 python market_climate.py  # 母集団を絞ってお試し実行
python ffty_screener.py     # FFTY候補一覧を表示 (+Sheets設定時は書き込みも)
python backtest_trend_state.py --ticker ^IXIC --period 5y  # FTD判定の過去検証
python -m pytest tests/ -v  # 計算ロジックの単体テスト(合成データ+モック、48件)
```

`python market_climate.py` はSheetsの環境変数が未設定でも、履歴JSON
(`docs/data/history/`)への保存は常に行う(git add/commit/pushまではしない。
それはGitHub Actionsワークフロー側の役割)。

---

## 5. 今後の拡張候補

- ~~ブレッドスの母集団をS&P500以外に広げる~~ → 対応済み(NASDAQ Trader経由)
- ~~セクター4分類の多数決だけを地合いの主判定にする~~ → 2026.09.14、
  O'Neil/IBD式フォロースルー・デイ(FTD)判定を主判定に追加し、セクター
  多数決は参考表示に格下げ
- ~~Google Sheetsへの書き込みが日付重複する~~ → upsert化で対応済み
- ~~履歴を蓄積してダッシュボード化する~~ → `history_store.py` +
  `docs/index.html`(GitHub Pages)で対応済み。ただし**個別銘柄OHLCVの
  「差分取得」化(毎日全銘柄を再ダウンロードせず、前日分に対して当日分だけ
  追加する)は今回のスコープに含めていない**。実現すれば実行時間・
  yfinanceへの負荷ともに大きく削減できるはずだが、数千銘柄分の価格系列を
  どう永続化するか(1ファイル/銘柄だと数千ファイルになる、等)の設計が
  別途必要で、今回は見送った
- FTD判定・「安値圏引け」・セクター温度感4分類の閾値を、
  `backtest_trend_state.py`や実際のオニミネの判定と突き合わせてチューニングする
  (現状は原典の定義または自前設計の初期値のまま)
- 経済イベントカレンダー(FOMC/CPI/PPI/雇用統計)を、公式発表日程を都度取得
  する形で実装する(日付の手打ちはしない)
- FFTY以外の銘柄プール(例: 全米国株からのミネルヴィニ・スクリーニング)を
  別スクリプトとして追加する
- スクリーニング結果と実際の値動き(その後ブレイクアウトしたか)を突き合わせる
  ダッシュボードを別途作る(トラッカーとしての振り返り用途)
- ダッシュボード(`docs/index.html`)に累積A/Dラインなどの長期推移グラフを追加する
  (現状は直近の地合い判定の色分けストリップのみ)
